#!groovy
// this means libraries download and accessible at run time
@Library('roboshop-library') _
def configMap = [
    application: "nodeJSVM",
    component: "catalogue"
]

// this is .grrovy filename and fucntion inside it
pipelinedecision.decidepipeline(configMap)